<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>{{ title }}</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, sans-serif; margin: 24px; }
    table { border-collapse: collapse; margin: 16px 0; width: 100%; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
    th { background: #f6f6f6; }
    .section { margin: 24px 0; }
    .small { color: #666; font-size: 12px; }
    img { max-width: 100%; height: auto; }
  </style>
</head>
<body>
  <h1>{{ title }}</h1>

  <div class="section">
    <h2>Summary Visualization (pH)</h2>
    <img src="{{ timeseries_image }}" alt="Timeseries plot" />
  </div>

  <div class="section">
    <h2>ANOVA by Site Type</h2>
    <table>
      <thead>
        <tr>
          <th>Parameter</th>
          <th>F-stat</th>
          <th>P-value</th>
          <th>Group Sizes</th>
        </tr>
      </thead>
      <tbody>
        {% for r in anova_results %}
        <tr>
          <td>{{ r.parameter }}</td>
          <td>{{ '%.4f' % r.f_stat }}</td>
          <td>{{ '%.6f' % r.p_value }}</td>
          <td>{{ r.group_sizes }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
    <p class="small">P-values >= 0.05 indicate no statistically significant difference between groups at the 95% confidence level.</p>
  </div>

  <div class="section">
    <h2>Regression Summary</h2>
    <table>
      <thead>
        <tr>
          <th>Parameter</th>
          <th>R²</th>
          <th>Coefficients</th>
          <th>P-values</th>
        </tr>
      </thead>
      <tbody>
        {% for r in regression_results %}
        <tr>
          <td>{{ r.parameter }}</td>
          <td>{{ '%.4f' % r.r2 }}</td>
          <td><pre>{{ r.coef | tojson(indent=2) }}</pre></td>
          <td><pre>{{ r.pvalues | tojson(indent=2) }}</pre></td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  <p class="small">Generated by OpenAcidMineDrainage.</p>
</body>
</html>

